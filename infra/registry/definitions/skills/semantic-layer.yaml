name: semantic-layer
description: "Query the semantic-layer ontologies in natural language (VKG → SPARQL/SQL, Semantic-RAG → retrieval). Per-user RBAC enforced via the semantic-layer Cognito pool."
skill_md_path: semantic-layer/SKILL.md
source: gateway
sourceRecord: semantic-layer
tools:
  - ListOntologies
  - OntologyQuery
  - MetadataQuery
  - QuerySuggestions
