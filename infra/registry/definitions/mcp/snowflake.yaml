name: snowflake
description: Snowflake Managed MCP Server (Cortex Analyst over REVENUE_VIEW)
display_name: Snowflake
tools:
  - name: revenue-analyst
    description: Query revenue data using natural language. Cortex Analyst generates SQL against the REVENUE_VIEW semantic view; on trial accounts the generated SQL is returned without result rows.
    input_type: object
    input_description: Natural language question about revenue data
    properties:
      - {
          name: message,
          type: string,
          description: Natural language question about revenue data,
          required: true,
        }
